# Phase 40E — Reader UX Polish Candidate Plan

Phase 40E is plan-only.

It creates a prioritized reader UX polish backlog after Phase 40D accepted the footer/button labels.

Preserved baseline:

```text
Home -> Files/Library -> Reader -> Back -> Files/Library -> Reopen Reader
```

Preserved footer:

```text
Back Select Open Stay
```

Recommended next actual patch:

```text
Phase 40F — Library Title Layout Polish Patch
```

Primary candidate:

```text
Library title layout consistency
```

Guardrails:

```text
- Do not change footer label text.
- Do not change input mapping or ADC thresholds.
- Do not change write lane.
- Do not change display geometry/rotation.
- Do not change EPUB title source/cache behavior.
```
