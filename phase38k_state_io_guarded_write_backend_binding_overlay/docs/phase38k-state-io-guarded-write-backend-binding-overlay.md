# Phase 38K — State I/O Guarded Write Backend Binding Overlay

This repair keeps Phase 38K as a policy-only write/backend binding scaffold and removes text that triggered the live-behavior guard. It also repairs the Phase 38I `.EPU`/`.EPUB` helper if an earlier overlay left it recursively calling itself.

Marker: `phase38k=x4-state-io-guarded-write-backend-binding-ok`
