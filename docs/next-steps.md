# Next Steps After Bootstrap

1. refine HAL trait signatures against real X4 code
2. port X4 capability flags into `hal-xteink-x4`
3. add a minimal boot shell in `target-xteink-x4`
4. define book fingerprint and cache directory format in `core`
5. only then start moving portable logic from `x4-reader-os-rs`
