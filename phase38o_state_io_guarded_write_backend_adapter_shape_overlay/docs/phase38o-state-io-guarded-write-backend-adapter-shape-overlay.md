# Phase 38O — State I/O Guarded Write Backend Adapter Shape Overlay

This phase defines the adapter shape for a future persistent typed-state mutation backend.

It introduces:
- adapter availability
- capability gates
- adapter operation kinds
- adapter request/plan models
- default unbound adapter

Expected marker:

```text
phase38o=x4-state-io-guarded-write-backend-adapter-shape-ok
```

Live mutation remains disabled and no persistent backend is bound in this phase.
